import 'package:flutter/material.dart';

class Addbox extends StatelessWidget {
  const Addbox({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Image or Video Upload page'),
    );
  }
}
